extends layout

block content
  div(class="space-y-2")
    h1(class="text-4xl")= title
    if category_list.length
      ul
        each category in category_list
          li
            a(href=category.url class="text-blue-700 hover:text-blue-800") #{category.name}

    else
      p There are no categories.